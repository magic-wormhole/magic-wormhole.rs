version: 2
jobs:
  build:
    docker:
      - image: circleci/rust:1.32
    steps:
      - checkout
      - restore_cache:
          key: project-cache
      - run:
          name: build
          command: cargo build
      - run:
          name: test
          command: cargo test
      - save_cache:
          key: project-cache
          paths:
            - "~/.cargo"
            - "./target"
